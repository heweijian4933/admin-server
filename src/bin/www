/**
 * 封装http层
 */
const { APP_PORT } = require('../config')

const app = require('../app')

app.listen(APP_PORT, () => {
  console.log(`admin server is running at port ${APP_PORT}`);
  console.log(`environment ${process.env.NODE_ENV}`);
})